import XCTest
@testable import GithubActionsIOS

final class GithubActionsIOSTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(GithubActionsIOS().text, "Hello, World!")
    }
}
